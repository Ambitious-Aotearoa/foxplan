{# templates/_sections/newsletter/newsletter-dollar-and-sense.twig #}

{# 1. Setup Scoped Color Defaults #}
{% set defaults = {
    heading: 'The Dollars and Sense Newsletter',
    description: 'Don’t have time to listen? Get the low-down on the latest episode of Dollars and Sense with our regular episode newsletter.',
    backgroundColor: 'bg-white',
    headingColor: 'text-gray-900',
    bodyColor: 'text-black',
    borderColor: 'border-gray-300',
    accentColor: 'text-gray-500'
} %}

{% set sectionData = defaults | merge(data ?? {}) %}

<section class="w-full {{ sectionData.backgroundColor }} pt-16 pb-16 md:pt-24 md:pb-24">
    <div class="px-8 md:px-12 w-full">

        {# Heading #}
        <h2 class="font-serif text-5xl md:text-7xl lg:text-8xl font-normal {{ sectionData.headingColor }} leading-[0.85] tracking-tight mb-6 max-w-4xl">
            {{ sectionData.heading }}
        </h2>

        {# Description #}
        <p class="text-lg md:text-xl {{ sectionData.bodyColor }} leading-relaxed max-w-xl mb-12 opacity-90">
            {{ sectionData.description }}
        </p>

        {# Horizontal Divider #}
        <div class="w-full border-t {{ sectionData.borderColor }} mb-16"></div>

        {# Static Articles Grid #}
        <div class="grid grid-cols-1 md:grid-cols-3 gap-x-12 gap-y-16">

            {# --- ARTICLE 1 --- #}
            <div class="group">
                <div class="w-full aspect-[5/3] mb-6 overflow-hidden bg-gray-100">
                    <img src="{{ craft.vite.asset('images/blog-1.jpg') }}" alt="Article 1" class="w-full h-full object-cover">
                </div>
                <p class="text-xs font-medium uppercase tracking-widest mb-3 {{ sectionData.accentColor }}">JUNE 13, 2024</p>
                <a href="#" class="block mb-4">
                    <h3 class="text-xl md:text-2xl font-serif leading-tight {{ sectionData.headingColor }} group-hover:opacity-70 transition-opacity">
                        Why choose private health insurance?
                    </h3>
                </a>
                <a href="#" class="inline-block text-xs font-bold uppercase tracking-widest border-b-2 border-transparent hover:border-current transition-all {{ sectionData.accentColor }}">
                    READ MORE
                </a>
            </div>

            {# --- ARTICLE 2 --- #}
            <div class="group">
                <div class="w-full aspect-[5/3] mb-6 overflow-hidden bg-gray-100">
                    <img src="{{ craft.vite.asset('images/blog-2.jpg') }}" alt="Article 2" class="w-full h-full object-cover">
                </div>
                <p class="text-xs font-medium uppercase tracking-widest mb-3 {{ sectionData.accentColor }}">MAY 20, 2024</p>
                <a href="#" class="block mb-4">
                    <h3 class="text-xl md:text-2xl font-serif leading-tight {{ sectionData.headingColor }} group-hover:opacity-70 transition-opacity">
                        8 great investment mistakes to avoid
                    </h3>
                </a>
                <a href="#" class="inline-block text-xs font-bold uppercase tracking-widest border-b-2 border-transparent hover:border-current transition-all {{ sectionData.accentColor }}">
                    READ MORE
                </a>
            </div>

            {# --- ARTICLE 3 --- #}
            <div class="group">
                <div class="w-full aspect-[5/3] mb-6 overflow-hidden bg-gray-100">
                    <img src="{{ craft.vite.asset('images/blog-3.jpg') }}" alt="Article 3" class="w-full h-full object-cover">
                </div>
                <p class="text-xs font-medium uppercase tracking-widest mb-3 {{ sectionData.accentColor }}">MAY 16, 2024</p>
                <a href="#" class="block mb-4">
                    <h3 class="text-xl md:text-2xl font-serif leading-tight {{ sectionData.headingColor }} group-hover:opacity-70 transition-opacity">
                        The ultimate guide to retirement planning
                    </h3>
                </a>
                <a href="#" class="inline-block text-xs font-bold uppercase tracking-widest border-b-2 border-transparent hover:border-current transition-all {{ sectionData.accentColor }}">
                    READ MORE
                </a>
            </div>

        </div>
    </div>
</section>