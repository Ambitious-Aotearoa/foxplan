{# _sections/explore-1.twig #}

{% set sectionData = {
    mainHeading: 'Explore insurance',
    services: [
        {
            title: 'General insurance',
            description: 'Insurance for protecting your assets – like your car, your home or your holiday batch from life’s little accidents.',
            linkText: 'Learn more',
            linkUrl: '#'
        },
        {
            title: 'Personal insurance',
            description: 'Protect yourself with our options for life insurance, income protection and health insurance that lower your risk.',
            linkText: 'Learn more',
            linkUrl: '#'
        },
        {
            title: 'Business insurance',
            description: 'Protect your business from costly mistakes and accidents with public liability, professional indemnity and other insurance.',
            linkText: 'Learn more',
            linkUrl: '#'
        }
    ]
} %}

{% set mainHeadingClasses = 'font-serif text-6xl lg:text-[4.5rem] font-light leading-none text-gray-900 pr-12' %}
{% set serviceTitleClasses = 'font-serif text-3xl md:text-4xl font-light text-gray-900 mb-3' %}
{% set serviceDescriptionClasses = 'text-base font-light text-gray-700 leading-relaxed max-w-sm mb-8' %}


<section class="w-full bg-gray-50 pt-16 pb-16 md:pt-24 md:pb-24">
    <div class="mx-auto px-4">

        {# Main Grid Container: 4 columns total (1 for the heading, 3 for the content) #}
        <div class="grid grid-cols-1 md:grid-cols-4 gap-x-24 items-start">

            {# Left Column: Main Heading (Takes 1 column) #}
            <div class="md:col-span-1 mb-12 md:mb-0 pt-4">
                <div class="max-w-xs lg:max-w-full">
                    <h2 class="{{ mainHeadingClasses }}">
                        {{ sectionData.mainHeading }}
                    </h2>
                </div>
            </div>

            {# Right Columns: Services Grid (Takes 3 columns) #}
            <div class="md:col-span-3">
                {# Nested Grid: 3 columns for the service blocks #}
                <div class="grid grid-cols-1 sm:grid-cols-3 gap-y-12 gap-x-8 lg:gap-x-12">

                    {# Iterate through all services #}
                    {% for service in sectionData.services %}
                    <div class="col-span-1">
                        {# FIX: Using h-full, flex, flex-col, and justify-between to push the button to the bottom for symmetrical alignment #}
                        <div class="h-full flex flex-col justify-between">

                            {# Content container #}
                            <div>
                                <h3 class="{{ serviceTitleClasses }}">
                                    {{ service.title }}
                                </h3>
                                <p class="{{ serviceDescriptionClasses }}">
                                    {{ service.description }}
                                </p>

                                {% include '_components/ui/button/button.twig' with {
                                    props: {
                                        text: service.linkText | default('Learn more'),
                                        style: 'primary',
                                        url: service.linkUrl | default('#'),
                                    }
                                } only %}
                            </div>


                        </div>
                    </div>
                    {% endfor %}

                </div>
            </div>

        </div>

    </div>
</section>